# .env.example - Docker Compose 环境变量示例配置
# 复制此文件为 .env 并根据实际情况修改

# ==================== Nginx API 代理配置 ====================
# API 服务运行在宿主机时，nginx 容器需要通过特殊地址访问宿主机

# MacOS/Windows Docker Desktop 用户使用 host.docker.internal
API_HOST=host.docker.internal

# Linux 用户需要使用宿主机在 Docker 网络中的 IP（通常是网关 IP）
# 可以通过以下命令查看：docker network inspect my-go-zero-looklook_looklook_net | grep Gateway
# API_HOST=172.16.0.1

# 如果 API 服务运行在容器中，使用容器服务名
# API_HOST=looklook

# ==================== Etcd 配置 ====================
# 宿主机 IP，用于 etcd 广播地址
# MacOS/Windows 用户可以使用 host.docker.internal
# Linux 用户需要设置为实际的宿主机 IP
HOST_IP=host.docker.internal
